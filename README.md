# External Movie Cache API

A FastAPI project for learning external API integration, caching, and database-backed API design using TMDb.

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python run.py
```
